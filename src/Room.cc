#include "Rooms.hh"

Room::Room(const char* n, const char* d)
{
  /*
  for (int x; x != 100; x++) {

    items[x] = i[x];
    
  }

  */
  name = n;
  desc = d;
  /*
  for(int x; x != 4; x++) {

    neighbours[x] = ns[x];
    
  }
  */
}
